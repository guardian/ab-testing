name: 🤔
on: [push]

jobs:
  jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: "10.x"  
      - run: lerna bootstrap
      - run: CI=true lerna run test
